
// This is the main angular declaration which declares the 'ng-app' attribute of 'body' tag //
// In '[]', the directives are declared //
// It binds html with angular //
// Note: The CDN for angular as well as the directives need to be declared in html //

var myApp = angular.module('booksApp',['ngRoute']);
